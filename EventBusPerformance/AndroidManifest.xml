<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.greenrobot.eventbusperf">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".jay.bus.App"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".jay.bus.BusTestActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.greenrobot.eventbusperf.TestRunnerActivity"
            android:label="@string/app_name"
            android:process="de.greenrobot.eventperf.benchmark"></activity>

        <activity android:name=".jay.bus.BusTest2Activity" />
        <activity android:name=".jay.bus.BusTest3Activity" />

    </application>

</manifest>